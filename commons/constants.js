const SMTP_HOST = 'servidorsmtp'
const SMTP_PORT = 465
const SMTP_USER = 'user@com.com'
const SMTP_PASS = 'password'
const SMTP_SECURE = true

module.exports = {
    SMTP_HOST: SMTP_HOST,
    SMTP_PORT: SMTP_PORT,
    SMTP_SECURE: SMTP_SECURE,
    SMTP_USER: SMTP_USER,
    SMTP_PASS: SMTP_PASS
}